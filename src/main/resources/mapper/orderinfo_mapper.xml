<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orderinfoMapper">
	<!-- 주문하기 -->
	<insert id="addorder" parameterType="orderinfovo">
		insert into orderinfo
		(orderid, userid, orderdate, totalprice, carid, receiptaddr,
		receiptlati, receiptlong)
		select ORDER_SEQ.NEXTVAL,
		#{userid}, sysdate, #{totalprice}, #{carid}, #{receiptaddr},
		#{receiptlati}, #{receiptlong}
		FROM DUAL
	</insert>
	
	<!-- 주문 상세 -->
	<select id="detailorder" resultType="orderinfovo">
		select d.prodid, d.prodnm, d.prodprice, d.ordercnt 
		from orderdetail d, orderinfo i 
		where i.orderid = d.orderid and d.orderid = #{orderid}
	</select>
	
	<!-- 주문 내역 -->
	<select id="orderinfo" resultType="orderinfovo">
		select i.orderid, i.userid, i.orderdate, i.receiptaddr, i.carid, i.totalprice
		from orderinfo i
		where i.userid = #{userid}
	</select>
</mapper>

<!-- 
OrderId
UserId
OrderDate
ReceiptAddr
CarId
TotalPrice


OrderId
PayCode
PayDate
PayPrice
UserId


ProdId
ProdNm
ProdPrice
OrderCnt
OrderPayInfo
OrderDetail
-->